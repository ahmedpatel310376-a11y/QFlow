import { AnimatePresence, motion, useSpring, useTransform } from 'framer-motion'
import {
  ArrowLeft, ArrowRight, Bell, CalendarDays, Check, ChevronRight,
  Clock3, DoorOpen, HeartPulse, History, MapPin, Menu, Search, Settings2,
  ShieldCheck, Sparkles, Ticket, UserRound, UsersRound, Zap,
} from 'lucide-react'
import { FormEvent, ReactNode, useEffect, useState } from 'react'
import { Link, useNavigate, useParams } from 'react-router-dom'
import {
  mockAppointments, mockCurrentToken, mockNotifications, mockOrganizations,
  mockQueueHistory, mockServices,
} from '../../data/mockData'
import { useToast } from '../../context/ToastContext'

const nav = [
  ['Overview', '/app', Sparkles], ['Find services', '/app/services', Search],
  ['My queue', '/app/queue/queue-001', Ticket], ['Appointments', '/app/appointments', CalendarDays],
  ['Notifications', '/app/notifications', Bell], ['History', '/app/history', History],
  ['Profile', '/app/profile', UserRound], ['Settings', '/app/settings', Settings2],
] as const

const pageMotion = { initial: { opacity: 0, y: 12 }, animate: { opacity: 1, y: 0 }, transition: { duration: 0.38, ease: 'easeOut' } } as const
const cardMotion = { initial: { opacity: 0, y: 14 }, animate: { opacity: 1, y: 0 }, transition: { duration: 0.38 } } as const

function Counter({ value, suffix = '' }: { value: number; suffix?: string }) {
  const spring = useSpring(0, { stiffness: 90, damping: 20 })
  const rounded = useTransform(spring, (latest) => `${Math.round(latest)}${suffix}`)
  useEffect(() => { spring.set(value) }, [spring, value])
  return <motion.span>{rounded}</motion.span>
}

function Shell({ title, eyebrow, children, action }: { title: string; eyebrow?: string; children: ReactNode; action?: ReactNode }) {
  const [open, setOpen] = useState(false)
  return <div className="min-h-screen bg-[#f7f8fc] text-slate-950">
    <aside className={`fixed inset-y-0 left-0 z-50 flex w-[272px] flex-col border-r border-slate-200/70 bg-white px-4 py-5 transition-transform duration-300 lg:translate-x-0 ${open ? 'translate-x-0' : '-translate-x-full'}`}>
      <Link to="/app" className="mb-9 flex items-center gap-3 px-3" onClick={() => setOpen(false)}><span className="grid h-10 w-10 place-items-center rounded-2xl bg-gradient-to-br from-indigo-600 to-violet-500 text-lg font-black text-white shadow-lg shadow-indigo-200">Q</span><span className="text-xl font-black tracking-tight">QFlow</span></Link>
      <p className="px-3 pb-3 text-[10px] font-bold uppercase tracking-[0.18em] text-slate-400">Workspace</p>
      <nav className="space-y-1">
        {nav.map(([label, href, Icon]) => <Link key={href} to={href} onClick={() => setOpen(false)} className={`group flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold transition ${location.pathname === href ? 'bg-indigo-600 text-white shadow-lg shadow-indigo-200' : 'text-slate-500 hover:bg-slate-100 hover:text-slate-900'}`}><Icon size={18} /><span>{label}</span>{label === 'Notifications' && <span className="ml-auto grid h-5 min-w-5 place-items-center rounded-full bg-rose-500 px-1 text-[10px] text-white">2</span>}</Link>)}
      </nav>
      <div className="mt-auto rounded-2xl bg-gradient-to-br from-indigo-50 to-violet-50 p-4"><div className="flex items-center gap-2 text-sm font-bold"><Sparkles size={15} className="text-indigo-600" /> Queue smarter</div><p className="mt-1.5 text-xs leading-5 text-slate-500">Get timely updates before you need to leave.</p></div>
    </aside>
    {open && <button aria-label="Close menu" onClick={() => setOpen(false)} className="fixed inset-0 z-40 bg-slate-950/20 lg:hidden" />}
    <main className="min-h-screen lg:pl-[272px]">
      <header className="sticky top-0 z-30 flex h-[76px] items-center justify-between border-b border-slate-200/70 bg-[#f7f8fc]/85 px-5 backdrop-blur-xl sm:px-8"><div className="flex items-center gap-3"><button onClick={() => setOpen(true)} className="rounded-xl p-2 text-slate-700 hover:bg-white lg:hidden"><Menu size={22} /></button><div><p className="text-xs font-semibold text-indigo-600">{eyebrow || 'QFLOW USER APP'}</p><h1 className="text-lg font-extrabold tracking-tight sm:text-xl">{title}</h1></div></div><div className="flex items-center gap-2 sm:gap-4">{action}<Link aria-label="Notifications" to="/app/notifications" className="relative rounded-xl bg-white p-2.5 text-slate-600 shadow-sm ring-1 ring-slate-200/80 transition hover:-translate-y-0.5 hover:text-indigo-600"><Bell size={18}/><span className="absolute right-1.5 top-1.5 h-2 w-2 rounded-full bg-rose-500 ring-2 ring-white" /></Link><Link to="/app/profile" className="hidden items-center gap-2 rounded-xl bg-white py-1.5 pl-1.5 pr-3 shadow-sm ring-1 ring-slate-200/80 sm:flex"><span className="grid h-8 w-8 place-items-center rounded-lg bg-violet-100 text-xs font-bold text-violet-700">AP</span><span className="text-sm font-bold">Ahmed</span></Link></div></header>
      <motion.div {...pageMotion} className="mx-auto max-w-7xl p-5 sm:p-8">{children}</motion.div>
    </main>
  </div>
}

function SectionTitle({ label, title, text, children }: { label?: string; title: string; text?: string; children?: ReactNode }) { return <div className="mb-6 flex flex-wrap items-end justify-between gap-3"><div>{label && <p className="mb-1 text-xs font-bold uppercase tracking-[0.16em] text-indigo-600">{label}</p>}<h2 className="text-2xl font-extrabold tracking-tight">{title}</h2>{text && <p className="mt-1 text-sm text-slate-500">{text}</p>}</div>{children}</div> }
function Pill({ children, color = 'indigo' }: { children: ReactNode; color?: 'indigo' | 'emerald' | 'amber' | 'rose' }) { const cls = { indigo: 'bg-indigo-50 text-indigo-700', emerald: 'bg-emerald-50 text-emerald-700', amber: 'bg-amber-50 text-amber-700', rose: 'bg-rose-50 text-rose-700' }[color]; return <span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-bold ${cls}`}>{children}</span> }
function PrimaryLink({ to, children }: { to: string; children: ReactNode }) { return <Link to={to} className="inline-flex items-center justify-center gap-2 rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-bold text-white shadow-lg shadow-indigo-200 transition hover:-translate-y-0.5 hover:bg-indigo-700 active:translate-y-0">{children}<ArrowRight size={16}/></Link> }

export function Dashboard() {
  return <Shell title="Good morning, Ahmed" eyebrow="Tuesday, August 18"><div className="grid gap-5 lg:grid-cols-[1.45fr_.9fr]">
    <motion.section {...cardMotion} className="overflow-hidden rounded-[28px] bg-gradient-to-br from-indigo-700 via-indigo-600 to-violet-600 p-6 text-white shadow-xl shadow-indigo-200 sm:p-8"><Pill color="indigo"><span className="h-1.5 w-1.5 rounded-full bg-emerald-400" /> LIVE QUEUE</Pill><div className="mt-6 flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><div><p className="text-sm text-indigo-100">You’re checked in at</p><h2 className="mt-1 text-3xl font-black tracking-tight">CityCare Hospital</h2><p className="mt-1 text-sm text-indigo-100">General OPD · Counter 2</p></div><PrimaryLink to="/app/queue/queue-001">View token</PrimaryLink></div><div className="mt-7 grid grid-cols-3 border-t border-white/15 pt-5"><div><p className="text-xs text-indigo-200">YOUR TOKEN</p><p className="mt-1 text-xl font-black">A-104</p></div><div><p className="text-xs text-indigo-200">POSITION</p><p className="mt-1 text-xl font-black"><Counter value={7} /></p></div><div><p className="text-xs text-indigo-200">EST. WAIT</p><p className="mt-1 text-xl font-black"><Counter value={24} suffix="m" /></p></div></div></motion.section>
    <motion.section {...cardMotion} transition={{ delay: .08 }} className="rounded-[28px] bg-white p-6 shadow-sm ring-1 ring-slate-200/70"><div className="flex items-center justify-between"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-amber-50 text-amber-600"><CalendarDays size={21}/></div><Pill color="amber">In 2 days</Pill></div><p className="mt-6 text-xs font-bold uppercase tracking-wider text-slate-400">Next appointment</p><h3 className="mt-1 text-lg font-extrabold">General OPD</h3><p className="mt-1 text-sm text-slate-500">CityCare Hospital · Aug 20, 11:30</p><Link to="/app/appointments" className="mt-5 inline-flex items-center gap-1 text-sm font-bold text-indigo-600">Manage appointment <ChevronRight size={16}/></Link></motion.section>
  </div>
  <div className="mt-8 grid gap-5 md:grid-cols-3">{[{icon: Clock3, n: 24, suffix: ' min', title: 'Time saved', text: 'compared to in-person waiting', c: 'text-indigo-600 bg-indigo-50'}, {icon: Ticket, n: 3, title: 'Visits this month', text: 'one active queue today', c: 'text-violet-600 bg-violet-50'}, {icon: HeartPulse, n: 92, suffix: '%', title: 'On-time service', text: 'across your recent visits', c: 'text-emerald-600 bg-emerald-50'}].map((m, i) => <motion.div {...cardMotion} transition={{delay: .14 + i * .06}} key={m.title} className="rounded-3xl bg-white p-5 shadow-sm ring-1 ring-slate-200/70"><div className={`grid h-11 w-11 place-items-center rounded-2xl ${m.c}`}><m.icon size={21}/></div><p className="mt-5 text-2xl font-black"><Counter value={m.n} suffix={m.suffix}/></p><p className="mt-1 text-sm font-bold">{m.title}</p><p className="mt-1 text-xs text-slate-500">{m.text}</p></motion.div>)}</div>
  <div className="mt-10 grid gap-7 lg:grid-cols-[1.35fr_.9fr]"><section><SectionTitle label="Discover" title="Make your next visit effortless" text="Find a service with the shortest wait, before you leave."/><div className="grid gap-4 sm:grid-cols-2">{mockOrganizations.slice(0, 2).map((org, i) => <motion.div {...cardMotion} transition={{delay: .2 + i * .07}} key={org.id} className="group rounded-3xl bg-white p-5 shadow-sm ring-1 ring-slate-200/70 transition hover:-translate-y-1 hover:shadow-lg"><div className="flex items-start justify-between"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-slate-50 text-2xl">{org.logo}</span><Pill color={org.currentCrowdLevel === 'high' ? 'rose' : 'emerald'}>{org.currentCrowdLevel === 'high' ? 'Busy now' : 'Low crowd'}</Pill></div><h3 className="mt-5 font-extrabold">{org.name}</h3><p className="mt-1 truncate text-sm text-slate-500">{org.address}</p><Link to={`/app/organization/${org.id}`} className="mt-4 inline-flex items-center gap-1 text-sm font-bold text-indigo-600">Explore services <ChevronRight size={16}/></Link></motion.div>)}</div></section><section className="rounded-3xl bg-white p-5 shadow-sm ring-1 ring-slate-200/70"><div className="flex items-center justify-between"><h3 className="font-extrabold">Recent activity</h3><Link to="/app/history" className="text-xs font-bold text-indigo-600">View all</Link></div><div className="mt-4 divide-y divide-slate-100">{mockQueueHistory.slice(0, 3).map((item) => <div key={item.id} className="flex items-center gap-3 py-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-emerald-50 text-emerald-600"><Check size={16}/></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{item.serviceName}</p><p className="text-xs text-slate-500">{item.organizationName} · {item.waitingTime} min wait</p></div><span className="text-xs text-slate-400">{item.date.slice(5)}</span></div>)}</div></section></div>
  </Shell>
}

export function FindServices() { const [query, setQuery] = useState(''); const [filter, setFilter] = useState('All'); const filtered = mockOrganizations.filter((org) => (filter === 'All' || org.type === filter.toLowerCase()) && org.name.toLowerCase().includes(query.toLowerCase())); return <Shell title="Find a service" eyebrow="Plan ahead, wait less"><SectionTitle title="Choose where you’d like to go" text="Live crowd levels and accurate waiting times, all in one place."/><div className="mb-7 flex flex-col gap-3 rounded-2xl bg-white p-3 shadow-sm ring-1 ring-slate-200/70 sm:flex-row"><label className="flex flex-1 items-center gap-3 rounded-xl bg-slate-50 px-3"><Search size={19} className="text-slate-400"/><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search hospitals, banks, services..." className="w-full bg-transparent py-3 text-sm outline-none placeholder:text-slate-400"/></label><button className="inline-flex items-center justify-center gap-2 rounded-xl px-4 py-2 text-sm font-bold text-slate-600 hover:bg-slate-50"><MapPin size={17}/> Near me</button></div><div className="mb-6 flex gap-2 overflow-x-auto pb-1">{['All', 'Hospital', 'Bank', 'College', 'Office', 'Government'].map((item) => <button key={item} onClick={() => setFilter(item)} className={`whitespace-nowrap rounded-full px-4 py-2 text-sm font-bold transition ${filter === item ? 'bg-indigo-600 text-white shadow-md shadow-indigo-200' : 'bg-white text-slate-500 ring-1 ring-slate-200 hover:text-slate-900'}`}>{item}</button>)}</div><motion.div layout className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{filtered.map((org, index) => { const services = mockServices.filter((s) => s.organizationId === org.id); return <motion.article layout {...cardMotion} transition={{delay: index * .05}} key={org.id} className="overflow-hidden rounded-3xl bg-white p-5 shadow-sm ring-1 ring-slate-200/70 transition hover:-translate-y-1 hover:shadow-xl"><div className="flex items-start justify-between"><span className="grid h-14 w-14 place-items-center rounded-2xl bg-slate-50 text-3xl">{org.logo}</span><Pill color={org.currentCrowdLevel === 'high' ? 'rose' : org.currentCrowdLevel === 'moderate' ? 'amber' : 'emerald'}><span className="h-1.5 w-1.5 rounded-full bg-current" />{org.currentCrowdLevel} crowd</Pill></div><h3 className="mt-5 text-lg font-extrabold">{org.name}</h3><p className="mt-1 flex items-center gap-1 text-sm text-slate-500"><MapPin size={14}/>{org.address.split(',')[0]}</p><div className="mt-5 space-y-2">{services.length ? services.slice(0, 2).map((service) => <div key={service.id} className="flex items-center justify-between rounded-xl bg-slate-50 px-3 py-2.5"><span className="text-sm font-semibold">{service.name}</span><span className="text-xs font-bold text-indigo-600">~{service.estimatedWaitTime} min</span></div>) : <p className="rounded-xl bg-slate-50 p-3 text-sm text-slate-500">Service details available soon</p>}</div><Link to={`/app/organization/${org.id}`} className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4 text-sm font-bold text-indigo-600">View organization <ArrowRight size={16}/></Link></motion.article>})}</motion.div>{!filtered.length && <div className="py-20 text-center text-slate-500">No services match that search.</div>}</Shell> }

export function OrganizationDetail() { const { id } = useParams(); const org = mockOrganizations.find((item) => item.id === id) || mockOrganizations[0]; const services = mockServices.filter((s) => s.organizationId === org.id); return <Shell title={org.name} eyebrow="Organization"><Link to="/app/services" className="mb-6 inline-flex items-center gap-2 text-sm font-bold text-slate-500 hover:text-indigo-600"><ArrowLeft size={16}/> All services</Link><section className="overflow-hidden rounded-[28px] bg-white shadow-sm ring-1 ring-slate-200/70"><div className="bg-gradient-to-r from-indigo-700 via-indigo-600 to-violet-600 p-6 text-white sm:p-8"><div className="flex flex-wrap items-start justify-between gap-4"><div className="flex items-center gap-4"><span className="grid h-16 w-16 place-items-center rounded-2xl bg-white/15 text-4xl backdrop-blur">{org.logo}</span><div><h2 className="text-2xl font-black">{org.name}</h2><p className="mt-1 flex items-center gap-1 text-sm text-indigo-100"><MapPin size={15}/>{org.address}</p></div></div><Pill color={org.currentCrowdLevel === 'high' ? 'rose' : 'emerald'}>{org.currentCrowdPercentage}% capacity</Pill></div><div className="mt-7 grid grid-cols-3 gap-3 border-t border-white/15 pt-5 text-sm"><span><strong className="block text-lg">{org.openingHours}</strong><small className="text-indigo-200">Opens</small></span><span><strong className="block text-lg">{org.closingHours}</strong><small className="text-indigo-200">Closes</small></span><span><strong className="block text-lg">{services.length || '—'}</strong><small className="text-indigo-200">Services</small></span></div></div><div className="p-6 sm:p-8"><SectionTitle title="Available services" text="Join a queue now, or reserve a time that works for you."/><div className="space-y-3">{services.length ? services.map((s, i) => <motion.div {...cardMotion} transition={{delay:i*.05}} key={s.id} className="flex flex-col gap-4 rounded-2xl border border-slate-100 p-4 transition hover:border-indigo-200 hover:bg-indigo-50/30 sm:flex-row sm:items-center"><div className="grid h-11 w-11 place-items-center rounded-xl bg-indigo-50 text-indigo-600"><HeartPulse size={20}/></div><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><h3 className="font-extrabold">{s.name}</h3><Pill color="emerald">Open</Pill></div><p className="mt-1 text-sm text-slate-500">{s.department} · {s.availableCounters} counters active · {s.currentQueueLength} waiting</p></div><div className="text-left sm:text-right"><p className="text-lg font-black text-indigo-600">~{s.estimatedWaitTime} min</p><p className="text-xs text-slate-400">estimated wait</p></div><div className="flex gap-2"><Link to={`/app/join-queue/${s.id}`} className="rounded-xl bg-indigo-600 px-3 py-2 text-sm font-bold text-white transition hover:bg-indigo-700">Join</Link><Link to={`/app/book-appointment/${s.id}`} className="rounded-xl bg-slate-100 px-3 py-2 text-sm font-bold text-slate-700 transition hover:bg-slate-200">Book</Link></div></motion.div>) : <p className="rounded-2xl bg-slate-50 p-6 text-slate-500">Services will be listed here shortly.</p>}</div></div></section></Shell> }

export function JoinQueue() { const { queueId } = useParams(); const navigate = useNavigate(); const { addToast } = useToast(); const service = mockServices.find((s) => s.id === queueId) || mockServices[0]; const org = mockOrganizations.find((o) => o.id === service.organizationId) || mockOrganizations[0]; const [step, setStep] = useState(1); const [agree, setAgree] = useState(false); const complete = () => { addToast(`Token A-104 is ready — you're in the queue.`, 'success'); navigate('/app/queue/queue-001') }; return <Shell title="Join the queue" eyebrow="Digital check-in"><div className="mx-auto max-w-3xl"><div className="mb-8 flex items-center justify-between">{['Review', 'Details', 'Confirmed'].map((name, i) => <div key={name} className="flex items-center gap-2"><span className={`grid h-8 w-8 place-items-center rounded-full text-xs font-black ${step > i ? 'bg-indigo-600 text-white' : 'bg-slate-200 text-slate-500'}`}>{step > i + 1 ? <Check size={15}/> : i + 1}</span><span className="hidden text-sm font-bold sm:inline">{name}</span>{i < 2 && <span className="hidden h-px w-14 bg-slate-200 sm:block"/>}</div>)}</div><AnimatePresence mode="wait">{step === 1 && <motion.section key="review" {...pageMotion} className="rounded-[28px] bg-white p-6 shadow-sm ring-1 ring-slate-200/70 sm:p-8"><Pill color="emerald">Service available</Pill><h2 className="mt-4 text-2xl font-black">{service.name}</h2><p className="mt-1 text-slate-500">{org.name} · {service.department}</p><div className="mt-7 grid gap-3 sm:grid-cols-3">{[[UsersRound, `${service.currentQueueLength}`, 'people waiting'], [Clock3, `~${service.estimatedWaitTime} min`, 'your estimated wait'], [DoorOpen, `${service.availableCounters}`, 'counters active']].map(([Icon, value, label]) => { const StatIcon = Icon as typeof Clock3; return <div key={String(label)} className="rounded-2xl bg-slate-50 p-4"><StatIcon size={19} className="text-indigo-600"/><p className="mt-3 font-black">{String(value)}</p><p className="text-xs text-slate-500">{String(label)}</p></div>})}</div><button onClick={() => setStep(2)} className="mt-7 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-indigo-600 px-4 py-3.5 text-sm font-bold text-white shadow-lg shadow-indigo-200 transition hover:bg-indigo-700">Continue to check in <ArrowRight size={17}/></button></motion.section>}{step === 2 && <motion.section key="details" {...pageMotion} className="rounded-[28px] bg-white p-6 shadow-sm ring-1 ring-slate-200/70 sm:p-8"><h2 className="text-2xl font-black">One more thing</h2><p className="mt-1 text-sm text-slate-500">We’ll use these preferences to keep your queue experience smooth.</p><label className="mt-6 block text-sm font-bold">Visit note <span className="font-normal text-slate-400">(optional)</span><textarea className="mt-2 min-h-24 w-full rounded-xl border border-slate-200 p-3 text-sm outline-none focus:border-indigo-400 focus:ring-4 focus:ring-indigo-50" placeholder="Anything the service team should know?"/></label><button onClick={() => setAgree(!agree)} className="mt-5 flex items-start gap-3 text-left"><span className={`mt-0.5 grid h-5 w-5 place-items-center rounded-md border ${agree ? 'border-indigo-600 bg-indigo-600 text-white' : 'border-slate-300'}`}>{agree && <Check size={14}/>}</span><span className="text-sm text-slate-600">I agree to receive queue updates and will arrive when notified.</span></button><div className="mt-7 flex gap-3"><button onClick={() => setStep(1)} className="rounded-xl bg-slate-100 px-4 py-3 text-sm font-bold">Back</button><button disabled={!agree} onClick={() => setStep(3)} className="flex-1 rounded-xl bg-indigo-600 px-4 py-3 text-sm font-bold text-white shadow-lg shadow-indigo-200 disabled:cursor-not-allowed disabled:opacity-40">Confirm queue entry</button></div></motion.section>}{step === 3 && <motion.section key="confirmed" {...pageMotion} className="rounded-[28px] bg-white p-8 text-center shadow-sm ring-1 ring-slate-200/70"><motion.div initial={{scale:0}} animate={{scale:1}} transition={{type:'spring', stiffness:180}} className="mx-auto grid h-20 w-20 place-items-center rounded-full bg-emerald-100 text-emerald-600"><Check size={38}/></motion.div><h2 className="mt-5 text-2xl font-black">You’re in!</h2><p className="mt-2 text-slate-500">Your digital token is being prepared.</p><div className="mx-auto mt-7 max-w-sm rounded-2xl bg-indigo-50 p-5"><p className="text-xs font-bold tracking-wider text-indigo-500">YOUR ESTIMATED WAIT</p><p className="mt-1 text-4xl font-black text-indigo-700">~{service.estimatedWaitTime} min</p></div><button onClick={complete} className="mt-7 rounded-xl bg-indigo-600 px-6 py-3 text-sm font-bold text-white shadow-lg shadow-indigo-200">View my token</button></motion.section>}</AnimatePresence></div></Shell> }

export function LiveQueue() { const { addToast } = useToast(); const [position, setPosition] = useState(mockCurrentToken.positionInQueue); const [wait, setWait] = useState(mockCurrentToken.estimatedWaitTime); const [paused, setPaused] = useState(false); useEffect(() => { if (paused) return; const id = window.setInterval(() => { setPosition((p) => Math.max(1, p - 1)); setWait((w) => Math.max(3, w - 3)) }, 9000); return () => window.clearInterval(id) }, [paused]); const progress = Math.min(100, Math.round((1 - position / 10) * 100)); return <Shell title="Your digital token" eyebrow="Live queue tracking" action={<Pill color="emerald"><span className="h-1.5 w-1.5 animate-pulse rounded-full bg-emerald-500"/> Live</Pill>}><div className="grid gap-6 lg:grid-cols-[1.3fr_.8fr]"><section className="relative overflow-hidden rounded-[30px] bg-slate-950 p-6 text-white shadow-2xl shadow-indigo-200 sm:p-8"><div className="absolute -right-16 -top-12 h-64 w-64 rounded-full bg-indigo-500/35 blur-3xl"/><div className="relative"><div className="flex items-center justify-between"><span className="rounded-full border border-white/15 bg-white/10 px-3 py-1 text-xs font-bold tracking-widest text-indigo-100">TOKEN</span><button onClick={() => { setPaused(!paused); addToast(paused ? 'Live updates resumed' : 'Live updates paused', 'info') }} className="rounded-lg bg-white/10 px-3 py-1.5 text-xs font-bold transition hover:bg-white/20">{paused ? 'Resume' : 'Pause'} updates</button></div><motion.p key={position} initial={{scale:.86, opacity:.5}} animate={{scale:1, opacity:1}} className="mt-8 text-7xl font-black tracking-tight sm:text-8xl">A-104</motion.p><p className="mt-1 text-sm text-slate-300">General OPD · CityCare Hospital</p><div className="mt-9 rounded-2xl border border-white/10 bg-white/10 p-5 backdrop-blur"><div className="flex justify-between text-xs font-bold text-indigo-100"><span>QUEUE PROGRESS</span><span>{progress}%</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-white/15"><motion.div animate={{width:`${progress}%`}} transition={{type:'spring', stiffness:55}} className="h-full rounded-full bg-gradient-to-r from-violet-300 to-cyan-300"/></div><p className="mt-3 text-sm text-slate-200">Counter 2 is currently serving <strong className="text-white">A-097</strong></p></div></div></section><section className="rounded-[30px] bg-white p-6 shadow-sm ring-1 ring-slate-200/70"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">You are</p><div className="mt-3 flex items-end gap-3"><motion.span key={position} initial={{y:-8, opacity:0}} animate={{y:0, opacity:1}} className="text-6xl font-black text-indigo-600">{position}</motion.span><span className="pb-2 text-sm font-medium text-slate-500">places away</span></div><div className="mt-8 border-t border-slate-100 pt-5"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Estimated wait</p><p className="mt-2 text-3xl font-black">~<Counter value={wait} suffix=" min" /></p><p className="mt-1 text-sm text-emerald-600">On track · updated just now</p></div></section></div><div className="mt-7 grid gap-5 lg:grid-cols-[1.3fr_.8fr]"><section className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200/70"><SectionTitle title="What happens next" text="We’ll notify you before it’s time to head in."/><div className="grid gap-3 sm:grid-cols-3">{[['Now', 'In line', 'Stay nearby'], ['Soon', 'Almost there', 'Get ready to go'], ['Your turn', 'Counter 2', 'Present your token']].map(([time, name, caption], i) => <div key={name} className={`rounded-2xl p-4 ${i === 0 ? 'bg-indigo-50 ring-1 ring-indigo-100' : 'bg-slate-50'}`}><p className="text-xs font-bold text-indigo-600">{time}</p><p className="mt-3 font-extrabold">{name}</p><p className="mt-1 text-xs text-slate-500">{caption}</p></div>)}</div></section><section className="rounded-3xl bg-indigo-50 p-6"><Zap size={22} className="text-indigo-600"/><h3 className="mt-4 font-extrabold">Smart arrival tip</h3><p className="mt-1 text-sm leading-6 text-slate-600">Leave when you’re 3 places away. We’ll send a notification.</p><button onClick={() => addToast('Arrival reminder enabled', 'success')} className="mt-4 text-sm font-bold text-indigo-600">Set reminder →</button></section></div></Shell> }

export function Appointments() { const { addToast } = useToast(); const [appointments, setAppointments] = useState(mockAppointments); const upcoming = appointments.filter(a => a.status === 'upcoming'); return <Shell title="Appointments" eyebrow="Plan your time"><SectionTitle title="Your scheduled visits" text="Everything you have coming up, in one place."><PrimaryLink to="/app/book-appointment/svc-001">Book appointment</PrimaryLink></SectionTitle><div className="grid gap-4">{upcoming.map((a, i) => <motion.article {...cardMotion} transition={{delay:i*.07}} key={a.id} className="flex flex-col gap-4 rounded-3xl bg-white p-5 shadow-sm ring-1 ring-slate-200/70 sm:flex-row sm:items-center"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-indigo-50 text-indigo-600"><CalendarDays size={25}/></div><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><h3 className="font-extrabold">{a.serviceName}</h3><Pill color="indigo">Upcoming</Pill></div><p className="mt-1 text-sm text-slate-500">{a.organizationName} · {a.department}</p></div><div className="sm:text-right"><p className="font-black">Aug {a.date.slice(-2)} · {a.time}</p><p className="mt-1 text-xs text-slate-400">ID {a.appointmentId}</p></div><button onClick={() => { setAppointments(prev => prev.map(item => item.id === a.id ? {...item, status: 'cancelled'} : item)); addToast('Appointment cancelled', 'info') }} className="rounded-xl bg-slate-100 px-3 py-2 text-sm font-bold text-slate-600 hover:bg-rose-50 hover:text-rose-600">Cancel</button></motion.article>)}</div><section className="mt-10"><SectionTitle title="Past appointments"/><div className="rounded-3xl bg-white shadow-sm ring-1 ring-slate-200/70">{appointments.filter(a => a.status !== 'upcoming').map(a => <div key={a.id} className="flex items-center justify-between border-b border-slate-100 p-5 last:border-0"><div><p className="font-bold">{a.serviceName}</p><p className="mt-1 text-sm text-slate-500">{a.organizationName} · {a.date}</p></div><Pill color={a.status === 'completed' ? 'emerald' : 'rose'}>{a.status}</Pill></div>)}</div></section></Shell> }

export function BookAppointment() { const { serviceId } = useParams(); const navigate = useNavigate(); const { addToast } = useToast(); const service = mockServices.find(s => s.id === serviceId) || mockServices[0]; const [date, setDate] = useState('2026-08-20'); const [time, setTime] = useState('11:30'); const [done, setDone] = useState(false); const times = ['09:00', '09:30', '10:15', '11:30', '12:00', '14:00']; const submit = (e: FormEvent) => { e.preventDefault(); setDone(true); addToast('Your appointment has been reserved.', 'success') }; return <Shell title="Book an appointment" eyebrow="Reserve your time"><div className="mx-auto max-w-3xl">{!done ? <form onSubmit={submit} className="rounded-[28px] bg-white p-6 shadow-sm ring-1 ring-slate-200/70 sm:p-8"><Pill color="indigo">{service.department || 'Service'}</Pill><h2 className="mt-3 text-2xl font-black">{service.name}</h2><p className="mt-1 text-slate-500">Pick a date and available time that suits your schedule.</p><label className="mt-8 block text-sm font-bold">Date<input type="date" value={date} min="2026-08-18" onChange={e => setDate(e.target.value)} className="mt-2 block w-full rounded-xl border border-slate-200 px-3 py-3 text-sm outline-none focus:border-indigo-400"/></label><p className="mt-7 text-sm font-bold">Available times</p><div className="mt-3 grid grid-cols-2 gap-3 sm:grid-cols-3">{times.map(t => <button type="button" onClick={() => setTime(t)} className={`rounded-xl border px-3 py-3 text-sm font-bold transition ${time === t ? 'border-indigo-600 bg-indigo-600 text-white shadow-lg shadow-indigo-100' : 'border-slate-200 text-slate-600 hover:border-indigo-300'}`} key={t}>{t}</button>)}</div><div className="mt-8 flex gap-3"><Link to="/app/services" className="rounded-xl bg-slate-100 px-5 py-3 text-sm font-bold">Cancel</Link><button className="flex-1 rounded-xl bg-indigo-600 px-5 py-3 text-sm font-bold text-white shadow-lg shadow-indigo-200">Confirm {date} · {time}</button></div></form> : <motion.section {...pageMotion} className="rounded-[28px] bg-white p-8 text-center shadow-sm ring-1 ring-slate-200/70"><div className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-emerald-100 text-emerald-600"><CalendarDays size={29}/></div><h2 className="mt-5 text-2xl font-black">You’re all booked</h2><p className="mt-2 text-slate-500">{service.name} on {date} at {time}</p><p className="mx-auto mt-5 max-w-sm rounded-xl bg-slate-50 p-4 text-sm text-slate-600">We’ll remind you 30 minutes before your appointment.</p><button onClick={() => navigate('/app/appointments')} className="mt-7 rounded-xl bg-indigo-600 px-5 py-3 text-sm font-bold text-white">View appointments</button></motion.section>}</div></Shell> }

export function Notifications() { const [items, setItems] = useState(mockNotifications); const unread = items.filter(item => !item.read).length; const markAll = () => setItems(prev => prev.map(item => ({...item, read:true}))); return <Shell title="Notifications" eyebrow={`${unread} unread updates`} action={<button onClick={markAll} className="text-sm font-bold text-indigo-600">Mark all read</button>}><SectionTitle title="Stay in the loop" text="Important service updates, right when you need them."/><div className="overflow-hidden rounded-3xl bg-white shadow-sm ring-1 ring-slate-200/70">{items.map((item, i) => <motion.button {...cardMotion} transition={{delay:i*.04}} onClick={() => setItems(prev => prev.map(current => current.id === item.id ? {...current, read:true} : current))} key={item.id} className={`flex w-full items-start gap-4 border-b border-slate-100 p-5 text-left transition last:border-0 hover:bg-slate-50 ${!item.read ? 'bg-indigo-50/45' : ''}`}><span className={`grid h-10 w-10 shrink-0 place-items-center rounded-xl ${item.type === 'delay' ? 'bg-amber-100 text-amber-600' : item.type === 'appointment' ? 'bg-violet-100 text-violet-600' : 'bg-indigo-100 text-indigo-600'}`}>{item.type === 'appointment' ? <CalendarDays size={18}/> : <Bell size={18}/>}</span><span className="min-w-0 flex-1"><span className="flex items-center gap-2"><strong className="text-sm">{item.title}</strong>{!item.read && <span className="h-2 w-2 rounded-full bg-indigo-600"/>}</span><span className="mt-1 block text-sm leading-5 text-slate-500">{item.message}</span><span className="mt-2 block text-xs text-slate-400">{item.createdAt.slice(0, 10)}</span></span></motion.button>)}</div></Shell> }

export function HistoryPage() { const [filter, setFilter] = useState('All'); const rows = mockQueueHistory.filter(item => filter === 'All' || item.status === filter); return <Shell title="Queue history" eyebrow="Your visits"><SectionTitle title="Every visit, remembered" text="A complete history of your queue activity."/><div className="mb-5 flex gap-2">{['All', 'completed', 'cancelled'].map(name => <button key={name} onClick={() => setFilter(name)} className={`rounded-full px-4 py-2 text-sm font-bold ${filter === name ? 'bg-indigo-600 text-white' : 'bg-white text-slate-500 ring-1 ring-slate-200'}`}>{name === 'All' ? 'All visits' : name}</button>)}</div><div className="overflow-hidden rounded-3xl bg-white shadow-sm ring-1 ring-slate-200/70">{rows.map((item, i) => <motion.div {...cardMotion} transition={{delay:i*.05}} key={item.id} className="flex flex-wrap items-center gap-4 border-b border-slate-100 p-5 last:border-0"><span className="grid h-11 w-11 place-items-center rounded-xl bg-slate-100 text-slate-600"><History size={19}/></span><div className="min-w-[180px] flex-1"><p className="font-extrabold">{item.serviceName}</p><p className="mt-1 text-sm text-slate-500">{item.organizationName} · {item.date}</p></div><div><p className="text-xs font-bold text-slate-400">TOKEN</p><p className="text-sm font-bold">{item.token}</p></div><div><p className="text-xs font-bold text-slate-400">WAIT</p><p className="text-sm font-bold">{item.waitingTime} min</p></div><Pill color={item.status === 'completed' ? 'emerald' : 'rose'}>{item.status}</Pill></motion.div>)}</div></Shell> }

export function Profile() { const { addToast } = useToast(); const [editing, setEditing] = useState(false); const [name, setName] = useState('Ahmed Patel'); return <Shell title="Your profile" eyebrow="Account"><div className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><section className="rounded-[28px] bg-gradient-to-br from-indigo-700 to-violet-600 p-7 text-white"><span className="grid h-20 w-20 place-items-center rounded-3xl bg-white/15 text-2xl font-black">AP</span><h2 className="mt-5 text-2xl font-black">{name}</h2><p className="mt-1 text-indigo-100">Member since January 2026</p><div className="mt-8 border-t border-white/15 pt-5"><p className="text-xs font-bold tracking-wider text-indigo-200">QFLOW MEMBER</p><p className="mt-2 flex items-center gap-2 font-bold"><ShieldCheck size={18}/> Verified account</p></div></section><section className="rounded-[28px] bg-white p-6 shadow-sm ring-1 ring-slate-200/70"><div className="flex items-center justify-between"><h2 className="text-lg font-extrabold">Personal details</h2><button onClick={() => setEditing(!editing)} className="text-sm font-bold text-indigo-600">{editing ? 'Discard' : 'Edit details'}</button></div><div className="mt-6 grid gap-5"><label className="text-sm font-bold">Full name<input disabled={!editing} value={name} onChange={e=>setName(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-3 font-normal outline-none disabled:opacity-70 focus:border-indigo-400"/></label><label className="text-sm font-bold">Email<input disabled={!editing} defaultValue="ahmed@example.com" className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-3 font-normal outline-none disabled:opacity-70"/></label><label className="text-sm font-bold">Phone<input disabled={!editing} defaultValue="+1 555 0123" className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-3 font-normal outline-none disabled:opacity-70"/></label></div>{editing && <button onClick={() => {setEditing(false); addToast('Profile saved successfully', 'success')}} className="mt-6 rounded-xl bg-indigo-600 px-5 py-3 text-sm font-bold text-white">Save changes</button>}</section></div></Shell> }

export function Settings() { const { addToast } = useToast(); const [settings, setSettings] = useState({ queue: true, appointment: true, sms: false, dark: false }); const toggle = (key: keyof typeof settings) => setSettings(prev => ({...prev, [key]:!prev[key]})); return <Shell title="Settings" eyebrow="Preferences"><SectionTitle title="Make QFlow yours" text="Control the updates and experience you receive."/><div className="overflow-hidden rounded-3xl bg-white shadow-sm ring-1 ring-slate-200/70">{[{key:'queue', title:'Queue updates', desc:'Get notified when your place in line changes.'}, {key:'appointment', title:'Appointment reminders', desc:'Receive timely reminders before booked visits.'}, {key:'sms', title:'SMS backup alerts', desc:'Send important queue updates by text message.'}, {key:'dark', title:'Dim appearance', desc:'Use a softer display for low-light environments.'}].map((item, i) => <motion.div {...cardMotion} transition={{delay:i*.05}} key={item.key} className="flex items-center gap-4 border-b border-slate-100 p-5 last:border-0"><span className="grid h-10 w-10 place-items-center rounded-xl bg-indigo-50 text-indigo-600">{i < 2 ? <Bell size={18}/> : <Settings2 size={18}/>}</span><div className="flex-1"><p className="font-bold">{item.title}</p><p className="mt-1 text-sm text-slate-500">{item.desc}</p></div><button aria-label={`Toggle ${item.title}`} onClick={() => {toggle(item.key as keyof typeof settings); addToast(`${item.title} updated`, 'success')}} className={`relative h-7 w-12 rounded-full transition ${settings[item.key as keyof typeof settings] ? 'bg-indigo-600' : 'bg-slate-200'}`}><motion.span animate={{x: settings[item.key as keyof typeof settings] ? 22 : 3}} className="absolute top-1 h-5 w-5 rounded-full bg-white shadow"/></button></motion.div>)}</div><button onClick={() => addToast('No sign-in sessions need attention.', 'info')} className="mt-6 flex w-full items-center justify-between rounded-2xl bg-white p-5 text-left shadow-sm ring-1 ring-slate-200/70"><span><strong className="block">Security & devices</strong><small className="text-slate-500">Manage your account security</small></span><ChevronRight className="text-slate-400"/></button></Shell> }
