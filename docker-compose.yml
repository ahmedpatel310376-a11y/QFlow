services:
  mongodb:
    image: mongo:7
    container_name: qflow-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - qflow_mongo_data:/data/db

volumes:
  qflow_mongo_data:
